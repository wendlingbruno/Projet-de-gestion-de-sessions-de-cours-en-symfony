{% extends 'base.html.twig' %}

{% block title %}Détail du stagiaire{% endblock %}

{% block contenu %}
<h3 class="textCentre">
  <svg width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-person" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
    <path fill-rule="evenodd" d="M10 5a2 2 0 1 1-4 0 2 2 0 0 1 4 0zM8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm6 5c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4zm-1-.004c-.001-.246-.154-.986-.832-1.664C11.516 10.68 10.289 10 8 10c-2.29 0-3.516.68-4.168 1.332-.678.678-.83 1.418-.832 1.664h10z"/>
  </svg>
  Fiche stagiaire
</h3>
{# <h3> {{ stagiaire.nom ~ " " ~ stagiaire.prenom }} </h3>

<p>sexe : {{ stagiaire.sexe }}</p>
<p>Date de naissance : {{ stagiaire.dateNaissance | date('d/m/Y')}}</p>
<p>Ville : {{ stagiaire.ville }}</p>
<p>Courriel : {{ stagiaire.email }}</p>
<p>Téléphone : {{ stagiaire.telephone }}</p>




<h3>Sessions en cours</h3>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateDebut < date() and sessions.dateFin > date() %}
    <a href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a> 
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}

  {% endif %}
{% endfor %}

<h3>Sessions à venir</h3>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateDebut > date() %}
    <a href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a>
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}

  {% endif %}
{% endfor %}


<h3>Sessions terminées</h3>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateFin < date() %}
    <a href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a>
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}

  {% endif %}
{% endfor %} #}
<div class="card text-white bg-primary mb-3 mx-auto" style="max-width: 30rem;">
  <div class="card-header"><h4>Profil d'un stagiaire</h4></div>
  <div class="card-body">
    <h4 class="card-title couleurNoir">{{ stagiaire.prenom ~ " " ~ stagiaire.nom | upper }} </h4>

<ul class="noStyle">
<li>Sexe : {{ stagiaire.sexe }}</li>
<li>Date de naissance : {{ stagiaire.dateNaissance | date('d/m/Y') ~ " (" ~ date().diff(date(stagiaire.dateNaissance)).y ~ " ans)" ~}}</li>
<li>Ville : {{ stagiaire.ville | upper }}</li>
<li>Courriel : {{ stagiaire.email }}</li>
<li>Téléphone : {{ stagiaire.telephone }}</li>
</ul>




<h3 class="couleurNoir">Sessions en cours</h3>
<ul>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateDebut < date() and sessions.dateFin > date() %}
    <li><a class="couleurfondbleu" href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a> 
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}</li>

  {% endif %}
{% endfor %}
</ul>

<h3 class="couleurNoir">Sessions à venir</h3>
<ul>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateDebut > date() %}
    <li><a class="couleurfondbleu" href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a>
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}</li>

  {% endif %}
{% endfor %}
</ul>


<h3 class="couleurNoir">Sessions terminées</h3>
<ul>
{% for sessions in stagiaire.sessions %}
  {% if sessions.dateFin < date() %}
    <li><a class="couleurfondbleu" href='{{ path("sessions_detail", {"id":sessions.id}) }}'>{{ sessions.nom }}</a>
    {{ " du " ~ sessions.dateDebut | date("d/m/Y") ~ " au " ~ sessions.dateFin | date("d/m/Y") }}</li>

  {% endif %}
{% endfor %}
</ul>
<a href={{ path("stagiaire_edit", {'id':stagiaire.id}) }}><button type="button" class="btn btn-dark">Modifier le stagiaire</button></a>
  </div>





{# {% for stagiaire in stagiaire.sessions %}

<p>
    {{ stagiaire.sessions ~ "(" ~ stagiaire.sessions.dateDebut ~ " " ~ stagiaire.sessions.dateFin ~ ")" ~ }}
</p>

    
{% endfor %} #}
{% endblock %}